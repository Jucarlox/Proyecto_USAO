import 'package:flutter/material.dart';

class SubeloScreen extends StatelessWidget {
  const SubeloScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Subelo"),
      ),
    );
  }
}
